
<div class="container">
	<div class="row" id="personal_row">
		<div class="col-xs-10">
			<div class="title-here">
				<h1> Pick what type of message you want to send.<span class="icon-color"><i class="fa fa-arrow-circle-o-down"></i></span></h1>
			</div>
				<div class="inner-logo-column">
				<% @letter.each do |letter| %>

				<div class= "letter">
					
				</div>
				<% end %>
				<%= form_tag new_letter_path, method: :get, :class => "letter-form" do %>
  				<div class="select-tag">
				  <%= select_tag :letter_type_id, options_from_collection_for_select(LetterType.where(category: 1), :id, :name), { :class => "select-control" } %>
				  <br>
				</div>
					
					<span id="element" class="element1"></span>
					<br>
					<div class="business-submit-div">
			  		<%= submit_tag "Let's go!", :class => "business-submit" %>
			  		</div>
					<% end %>
				</div>	
			</div>
		</div>
    </div>
</div>










