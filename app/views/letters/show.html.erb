<div class="backend">
<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @letter.name %>
</p>

<% @letter.properties.each do |name, value| %>
	<p>
		<b><%= name.humanize %>:</b>
		<%= value %>
	</p>
	<% end %>
	
<%= link_to 'Edit', edit_letter_type_path(@letter_type) %> |
<%= link_to 'Back', letter_types_path %>
</div>