<div class="container">
	<div class="row">
		
  		<div class="col-xs-6 col-md-4">
  			<span class = "element"></span>
  		</div>
  		<div class="col-xs-12 col-md-8">.
			<% @letter.each do |letter| %>

				<div class= "letter">
					
				</div>
				<% end %>

			<%= form_tag new_letter_path, method: :get do %>


			  <%= select_tag :letter_type_id, options_from_collection_for_select(LetterType.where(category: 2), :id, :name) %>
			  <br>
			  <%= submit_tag "Create Message" %>
			<% end %>
		</div>
	
	
 
 </div>
</div>